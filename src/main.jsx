import React from 'react'
import ReactDOM from 'react-dom/client'

const root = document.getElementById('root')

root.render(
  ReactDOM.createRoot(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
  )
)